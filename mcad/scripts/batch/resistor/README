Run generate.sh to generate the base output filenames, color code sequences,
and build all the models and place them in their directories.

To add more models, create parameter files and edit generate.sh.

For examples of parameter files, see the files in the params directory.

